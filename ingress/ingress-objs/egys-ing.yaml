---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: egys
  namespace: egys
  annotations:
    kubernetes.io/ingress.class: nginx                  # change to traefik if using traefik
    nginx.ingress.kubernetes.io/rewrite-target: /       # delete if using traefik
spec:
  tls:
    - hosts:
      - egys.dc01.havelsan.io
      secretName: dc01.havelsan.io
  rules:
  - host: egys.dc01.havelsan.io
    http:
      paths:
      - path: /
        backend:
          serviceName: egys-service
          servicePort: 8080
